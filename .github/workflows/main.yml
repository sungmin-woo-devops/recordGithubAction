name: helloGithubAction

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run test.py!
        run: python test.py
      - name: Find test.py!
        run: find / -name 'test.py'
